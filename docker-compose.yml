services:
  discord-music-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: discord_ai_music_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
